name: Build Xiaomi Pad 7 Kernel

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:

      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Install dependencies
        run: |
          set -e

          sudo add-apt-repository universe
          sudo apt-get update

          sudo apt-get install -y \
            bc \
            bison \
            build-essential \
            ccache \
            flex \
            git \
            lld \
            clang \
            llvm \
            libelf-dev \
            libssl-dev \
            zlib1g-dev \
            pkg-config \
            pahole \
            python3 \
            rsync \
            unzip \
            zip \
            curl

      - name: Check Xiaomi source
        run: |
          set -e

          echo "========================================"
          echo "      XIAOMI PAD 7 SOURCE CHECK"
          echo "========================================"

          echo "=== BRANCH ==="
          git branch --show-current

          echo "=== COMMIT ==="
          git rev-parse HEAD

          echo "=== SOURCE KERNEL VERSION ==="
          make -s kernelversion

          echo "=== STOCK CONFIG ==="
          test -f arch/arm64/configs/uke_stock.config
          echo "uke_stock.config FOUND"

          echo "=== STOCK CONFIG HEADER ==="
          head -5 arch/arm64/configs/uke_stock.config

      - name: Check stock config
        run: |
          set -e

          echo "========================================"
          echo "       STOCK CONFIG 6.1.138"
          echo "========================================"

          grep '^CONFIG_CC_VERSION_TEXT=' \
            arch/arm64/configs/uke_stock.config

          grep '^CONFIG_CLANG_VERSION=' \
            arch/arm64/configs/uke_stock.config

          grep '^CONFIG_LLD_VERSION=' \
            arch/arm64/configs/uke_stock.config

          grep '^CONFIG_TRIM_UNUSED_KSYMS=' \
            arch/arm64/configs/uke_stock.config

          grep '^CONFIG_MODULES=' \
            arch/arm64/configs/uke_stock.config

          grep '^CONFIG_MODVERSIONS=' \
            arch/arm64/configs/uke_stock.config

      - name: Setup KernelSU
        run: |
          set -e

          echo "========================================"
          echo "          KERNELSU SETUP"
          echo "========================================"

          rm -rf KernelSU

          git clone --depth=1 \
            https://github.com/tiann/KernelSU.git \
            KernelSU

          echo "=== RUN KERNELSU SETUP ==="

          bash KernelSU/kernel/setup.sh

          echo "=== CHECK KERNELSU ==="

          test -e drivers/kernelsu
          test -f drivers/kernelsu/Kconfig

          echo "KernelSU setup OK"

          

          cd ..

          bash KernelSU/kernel/setup.sh

          echo "=== CHECK KERNELSU ==="

          test -e drivers/kernelsu
          test -f drivers/kernelsu/Kconfig

          echo "KernelSU setup OK"

      - name: Clone SUSFS
        run: |
          set -e

          echo "========================================"
          echo "            SUSFS SETUP"
          echo "========================================"

          rm -rf susfs

          git clone --depth=1 \
            --branch gki-android14-6.1 \
            https://github.com/deepongi-labs/susfs4ksu.git \
            susfs

          echo "=== SUSFS COMMIT ==="

          git -C susfs log -1 --oneline

          echo "=== SUSFS PATCH ==="

          test -f \
            susfs/kernel_patches/50_add_susfs_in_gki-android14-6.1.patch

          test -f \
            susfs/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch

          echo "SUSFS patches FOUND"

      - name: Copy SUSFS files
        run: |
          set -e

          echo "========================================"
          echo "       COPY SUSFS FILES"
          echo "========================================"

          cp -fv \
            susfs/kernel_patches/fs/* \
            fs/

          cp -fv \
            susfs/kernel_patches/include/linux/* \
            include/linux/

      - name: Test KernelSU SUSFS patch
        run: |
          set -e

          echo "========================================"
          echo "       TEST KERNELSU SUSFS"
          echo "========================================"

          cd KernelSU

          patch --dry-run \
            -p1 \
            < ../susfs/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch

          cd ..

          echo "KernelSU SUSFS patch test PASSED"

      - name: Apply KernelSU SUSFS patch
        run: |
          set -e

          cd KernelSU

          patch \
            -p1 \
            < ../susfs/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch

          cd ..

          echo "KernelSU SUSFS patch applied"

      - name: Test kernel SUSFS patch
        run: |
          set -e

          echo "========================================"
          echo "       TEST KERNEL SUSFS PATCH"
          echo "========================================"

          patch --dry-run \
            -p1 \
            < susfs/kernel_patches/50_add_susfs_in_gki-android14-6.1.patch

          echo "SUSFS kernel patch matches source"

      - name: Apply kernel SUSFS patch
        run: |
          set -e

          patch \
            -p1 \
            < susfs/kernel_patches/50_add_susfs_in_gki-android14-6.1.patch

          echo "SUSFS kernel patch applied"

      - name: Prepare stock configuration
        run: |
          set -e

          export ARCH=arm64
          export SUBARCH=arm64

          echo "========================================"
          echo "       PREPARE STOCK CONFIG"
          echo "========================================"

          rm -rf out
          mkdir -p out

          cp \
            arch/arm64/configs/uke_stock.config \
            out/.config

          echo "=== ENABLE KERNELSU ==="

          scripts/config \
            --file out/.config \
            -e CONFIG_KSU

          echo "=== ENABLE SUSFS ==="

          scripts/config \
            --file out/.config \
            -e CONFIG_KSU_SUSFS

          echo "=== KEEP STOCK CONFIG ==="

          grep '^CONFIG_TRIM_UNUSED_KSYMS=' \
            out/.config || true

          echo "=== FINALIZE CONFIG ==="

          make \
            O=out \
            ARCH=arm64 \
            olddefconfig

          test -f out/.config
          test -f out/include/config/auto.conf

          echo "=== FINAL CONFIG ==="

          grep -E \
            '^(CONFIG_KSU=|CONFIG_KSU_SUSFS=|CONFIG_MODULES=|CONFIG_MODVERSIONS=|CONFIG_TRIM_UNUSED_KSYMS=)' \
            out/.config || true

      - name: Prepare kernel
        run: |
          set -e

          export ARCH=arm64
          export SUBARCH=arm64

          echo "========================================"
          echo "          KERNEL PREPARE"
          echo "========================================"

          make \
            O=out \
            ARCH=arm64 \
            prepare

          make \
            O=out \
            ARCH=arm64 \
            modules_prepare

      - name: Build Image
        run: |
          set -e

          export ARCH=arm64
          export SUBARCH=arm64

          echo "========================================"
          echo "             BUILD IMAGE"
          echo "========================================"

          make \
            -j"$(nproc)" \
            O=out \
            ARCH=arm64 \
            Image

          test -f \
            out/arch/arm64/boot/Image

          ls -lh \
            out/arch/arm64/boot/Image

      - name: Build DTBs
        run: |
          set -e

          export ARCH=arm64
          export SUBARCH=arm64

          echo "========================================"
          echo "              BUILD DTBS"
          echo "========================================"

          make \
            -j"$(nproc)" \
            O=out \
            ARCH=arm64 \
            dtbs

      - name: Verify result
        run: |
          set -e

          echo "========================================"
          echo "           BUILD SUCCESS"
          echo "========================================"

          test -f \
            out/arch/arm64/boot/Image

          echo "=== IMAGE ==="

          ls -lh \
            out/arch/arm64/boot/Image

          echo "=== KERNEL RELEASE ==="

          make \
            O=out \
            ARCH=arm64 \
            -s kernelrelease

          echo "=== DTB FILES ==="

          find \
            out/arch/arm64/boot/dts \
            -type f \
            \( -name "*.dtb" -o -name "*.dtbo" \) \
            -print | head -100 || true

      - name: Upload kernel
        uses: actions/upload-artifact@v4
        with:
          name: xiaomi-pad7-kernel-ksu-susfs
          path: |
            out/arch/arm64/boot/Image
            out/arch/arm64/boot/Image.gz
            out/arch/arm64/boot/dts/**/*.dtb
            out/arch/arm64/boot/dts/**/*.dtbo
          if-no-files-found: warn
